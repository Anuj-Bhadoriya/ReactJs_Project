function Score(props){
    return(
        <>
            <div>Results App</div>
            <div>Your Score is : {props.Scores} </div>
            <div>Total Score is : {props.totalQuestion}</div>

        </>
    )
};

export default Score;